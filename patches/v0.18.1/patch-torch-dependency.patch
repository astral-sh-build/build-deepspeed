diff --git a/setup.py b/setup.py
index 67befe3d..881b1a4e 100755
--- a/setup.py
+++ b/setup.py
@@ -281,6 +281,17 @@ torch_info = {
     "hip_version": hip_version
 }
 
+# Pin the PyTorch version.
+index = install_requires.index("torch")
+install_requires[index] = f"torch=={TORCH_MINOR}.{TORCH_MINOR}.*"
+for requirements in extras_require:
+    try:
+        index = requirements.index("torch")
+        requirements[index] = f"torch=={TORCH_MINOR}.{TORCH_MINOR}.*"
+    except ValueError:
+        pass
+
+
 print(f"version={version_str}, git_hash={git_hash}, git_branch={git_branch}")
 with open('deepspeed/git_version_info_installed.py', 'w') as fd:
     fd.write(f"version='{version_str}'\n")
@@ -291,6 +302,7 @@ with open('deepspeed/git_version_info_installed.py', 'w') as fd:
     fd.write(f"torch_info={torch_info}\n")
 
 print(f'install_requires={install_requires}')
+print(f'extras_require={extras_require}')
 print(f'ext_modules={ext_modules}')
 
 # Parse README.md to make long_description for PyPI page.
